# Data Directory

This folder stores large cloned repositories or local datasets used for experiments.

## Structure

- `repos/`: cloned public repositories used for backfill/testing.

Keeping repository snapshots here avoids cluttering `reports/` and project root.
